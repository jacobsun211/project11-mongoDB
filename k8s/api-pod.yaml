apiVersion: v1
kind: Pod
metadata:
  name: api
  labels:
    app: api
spec:
  containers:
  - name: mongodb
    image: jacobsun211/contacts-api:latest
    imagePullPolicy: Always
    ports:
    - containerPort: 8000
    env:
    - name: MONGO_HOST
      value: "mongodb-service"
    - name: MONGO_PORT
      value: "27018"
    - name: MONGO_DB
      value: "contactsdb"
    